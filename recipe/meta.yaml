{% set name = "rotary-embedding-torch" %}
{% set version = "0.8.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 691753c846b87f719a6a1394bd5a16137b8f8b57c1bccb2dff2975f6bb142a6c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - beartype
    - einops >=0.7
    - pytorch >=2.0

test:
  imports:
    - rotary_embedding_torch
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/lucidrains/rotary-embedding-torch
  summary: Rotary Embedding - Pytorch
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - leightonpayne
